import styles from "./header.module.css";

const Header = () => {
  return (
    <header>
      <nav className={styles.nav}>
        <h3>HEADER</h3>
      </nav>
    </header>
  );
};

export default Header;
