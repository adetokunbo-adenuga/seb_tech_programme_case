import styles from "./header.module.css";

const Header = () => {
  return (
    <header>
      <nav className={styles.nav}>
        <h2>HEADER</h2>
      </nav>
    </header>
  );
};

export default Header;
